# Kreivo Pass Signer

The `KreivoPassSigner` is the bridge between an Authenticator (SubstrateKey, WebAuthn) and the Polkadot API.

## Usage

::: code-group
<<< ./snippets/substrate/authenticate.ts [signer-usage.ts]
:::
